# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
ENV/

# IDEs
.idea/
.vscode/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/

# Type checking
.pyright/

# Tooling caches
.ruff_cache/
.complexipy_cache/

# Session-local artifacts (handoffs, scratch). Tracked elsewhere via
# .opencode/handoffs/ but never committed to the repo root.
.opencode/

# mutmut output directory (mutation testing).
mutants/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.log
smoke_test/
# uv.lock is intentionally NOT ignored — committed for reproducible CI builds.

# Baselines for the per-PR GPU gates (biolab-runners #151, #152).
# Override the smoke_test/ ignore for these specific files:
!smoke_test/baseline.json
!smoke_test/boltz2_baseline.json
